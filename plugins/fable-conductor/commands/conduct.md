---
description: Conduct a full work stream — Fable-led shaping/spec/planning, autonomous sonnet/haiku adversarial execution, Fable final review.
argument-hint: "[idea | path to spec/plan | stream dir]"
---

Invoke the `fable-conductor:conduct` skill, passing `$ARGUMENTS` through as the stream input.

If `$ARGUMENTS` is empty, the skill first scans `docs/fable-streams/` for an in-flight stream to resume; if none is found, it asks for the idea to conduct before proceeding.

Let the skill drive phase routing, weave detection, and orchestration from here.
